---
'strapi-plugin-sitetune': minor
---

Add Pillar E: reusable content-block components (`sitetune-blocks.testimonial`, `.team-member`, `.faq-item`, `.cta`), created on the host at boot the same way as `sitetune.seo`/`sitetune.open-graph`. Meant to be shared across any SiteTune site regardless of vertical (SEO tooling, real estate, tech consulting, personal training, …) — see `docs/SPEC-002-sitetune-content-blocks.md` for the full design.
